<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-new-eaten="{{eaten}}">
		{{#if eaten}}Eat Burger{{else}}Order Again{{/if}}
	</button>

	<button class="delete-burger delete" data-id="{{this.id}}">DELETE!</button>
</li>
